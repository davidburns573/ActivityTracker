package tech.davidburns.activitytracker.util

class SessionSchema {
    object SessionTable {
        const val NAME: String = "sessions"

        object Cols {
            val LENGTH = "length"
        }
    }
}